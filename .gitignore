# dependencies
node_modules

# local npm cache (avoids root-owned global cache)
.npm-cache

# build output
dist

# logs
*.log

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json

# amplify (when backend is added later)
.amplify
amplify_outputs*
amplifyconfiguration*
